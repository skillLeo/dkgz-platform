*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build is deliberately COMMITTED — the shared host has no npm.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Never commit environment secrets or the imported design working copy's state
.env.production
*.pem
*.key
auth.json

# Local SQLite scratch database — production uses MySQL
/database/database.sqlite

# SSR bundle for the page-render smoke test, rebuilt on every run
/storage/framework/testing
/storage/app/private/test-summary.json
